namespace MyWebsite.Domain.Enums
{
    public enum QuotationStatus
    {
        Pending, // Đang chờ
        Approved, // Đã duyệt
        Rejected, // Từ chối
        Completed // Hoàn thành
    }
}
